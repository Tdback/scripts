#!/bin/sh

# ==============================================================================
#
#   Author  : Tyler Dunneback
#   Date    : Aug 05, 2023
#   GitHub  : https://github.com/Tdback
#
#   Copy a file to a specific directory, then go to that directory.
#
# ==============================================================================

if [ -d "$2" ]
then
    cp "$1" "$2" && cd "$2" || exit 1
else
    cp "$1" "$2"
fi

# vim: ft=sh
